/**
 * Placeholder for the server-only Firebase session validation guard.
 * Implementation must verify a server-issued session cookie before allowing
 * access to private routes.
 */
export type AuthenticationGuard = never;
